Blocked: the required single PowerShell command `(Get-Location).Path` did not execute because the local Windows sandbox failed during process startup (`windows sandbox: spawn setup refresh`).
I did not run any additional shell commands, so there is no reliable printed working-directory path to record without guessing.
